developer_instructions = """
Review the recently changed React source under src/components, src/views, src/hooks, src/lib and src/app.tsx for the repo's anti-patterns, then fix the clear violations.
Repo-critical traps: useFeed, useCommunity, useCommunityStats and useCommunitiesStates take a CommunityIdentifier ({name} or {publicKey}) and silently return nothing for an address string, so route addresses through src/hooks/use-community-identifier.ts; useSubscribe is the exception and still takes a plain communityAddress string; any hook argument must be an object or undefined, because a falsy-non-null value such as (cond && options) throws mid-render and there is no error boundary.
Other violations to fix: useState for shared state (use a small zustand store in src/hooks, there is no src/stores directory), useEffect for data fetching, effects that sync derived state, boolean flag soup instead of the state value the protocol hook already returns, copy-pasted logic that belongs in a custom hook, effects without cleanup, and any lapse from strict TypeScript (a new .js/.jsx file under src/, an any, a @ts-ignore, a hand-rolled copy of a type @bitsocial/bitsocial-react-hooks exports, or props typed with React.FC instead of an interface XProps above the component).
Leave established correct effects alone, including the react-virtuoso scroll-position effects and the document.title effect in src/app.tsx.
Verify with yarn lint, yarn type-check, yarn build and yarn doctor, treating react-doctor as a reviewer of newly introduced diagnostics rather than a score to grind up. There is no test suite.
Report the files reviewed, the violations fixed, the violations you left and why, and the command results.
"""
